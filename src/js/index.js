// Entry Point:
// En este archivo importamos lo que se va a requerir para que la app funcione
import "../scss/index.scss"
import "../scss/search.scss"
import "../scss/meal.scss"

import {rutes} from "./rutes"; // Importamos la función rutes aquí. Debe ejecutarse siempre

rutes()